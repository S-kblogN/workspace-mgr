# End-to-end coverage contract

This matrix is the audit boundary for `workspace-mgr`. “System E2E” means the
compiled CLI is treated as an opaque program, Git traffic crosses a real
`git://` server, and stored content crosses the S3 API to a versioned MinIO
bucket. It is not a count of test cases: each row names the state transition or
failure invariant that must remain covered.

Small input-validation permutations and deterministic fault injection stay in
isolated integration or unit tests. Moving those cases into the system scenario
would make the network lifecycle slower without proving another boundary.

| Public behavior or invariant | System E2E coverage | Isolated coverage retained for variants |
| --- | --- | --- |
| Private runtime setup | Dry-run, real isolated installation of the pinned storage runtime, explicit ownership marker, executable verification, and idempotent reuse | Unmanaged-target refusal, failed replacement rollback, and concurrent setup lock |
| Repository initialization | Dry-run, full scaffold including the generated root ignore file, content-independent first-init collisions for the instruction, root-ignore, and storage paths with no partial files and the ignore collision naming its module, template-equal collision, verbatim import of the repository ignore module, idempotency, deterministic whole-file scaffold repair, refusal plus documented migration for a root ignore file the product did not generate, and marker-free internal-config repair with live S3 pointers while committed facts still prevent relocation | Partial engine failure rollback, exact-template collision, reserved internal-config collision, owned-file recreation, preservation of a managed local-only ignore block across regeneration and repair of an unpaired marker, refusal of an ignore module carrying the product's own markers, refusal of a symlinked root ignore file with its migration, removal of unused S3 configuration, and unsafe URLs |
| Effective policy | Every instruction topic, model-before-rules ordering, constrained repository-content module composition, human and JSON output, stable policy hashes, and fixed agent/user pull-request responsibilities | Documentation vocabulary and fixed-policy contract |
| Configuration and diagnostics | Public configuration contains exactly Git and S3 facts, exposes no strategy switches, hides the private engine, and cannot relocate existing S3 boundaries; doctor passes with versioned S3 and fails when versioning or managed config is unhealthy | Strict schema, URL, policy-key rejection, and release-build filesystem-remote validation |
| Deliverable task scaffold | Dry-run, creation, immediate-after-scaffold-publication review handoff, before-every-turn-end synchronization cadence, duplicate refusal without clobbering, manifest discovery from the task and explicit path, branch initialization, README whose directory map asks the task to keep its own record, refusal of a content publication that documents nothing, the `task-record-unchanged` warning clearing once the record is updated, and shared checkout staying on `main` | Required identity fields, exact ID/branch mapping, cross-clone branch ownership, invalid slug/timestamp, missing-README publication guard, a pre-upgrade README satisfying the documentation guard, a manifest title edit leaving the guard in place, retirement of task content and of its last record, an authorized extra scope publishing without a task record, infrastructure publication exempt from both, warning suppression for empty and housekeeping-only deltas, capped reported ignored paths, and the `bulk-publication` warning above the file and byte thresholds, silent at them, silent after a rename, and equal at plan and publish for an automatically placed boundary |
| Task slug rename | Published deliverable dry-run and apply, stable task/branch/review identity, complete directory and S3-metadata move, no remote writes before publication, old-tree deletion plus new-tree publication on the same network branch, published-placement preservation, path-bound S3 version rematerialization, permanent old-path purge, exact hydration from the renamed S3 path, and final clean plan | Unpublished and legacy-schema rename, invalid/same/colliding/staged/merged refusal, large published-Git placement preservation, cloud-version reset, infrastructure metadata rename, and rollback guards |
| Exceptional checkout | Unexpected checkout is refused; the explicit `--allow-non-shared-head` plus one-line authorization can plan | Target branch mounted in another worktree and malformed authorization text |
| Infrastructure task scaffold | Dry-run, private manifest, after-first-scoped-publication review handoff, before-every-turn-end synchronization cadence, isolated worktree, no task directory, exact declared scopes, undeclared-change refusal, shared private cache/config links, task-aware storage status, Git plus S3 publication, publication of a deleted file scope followed by a clean plan, and a clean worktree | Invalid/missing infrastructure scopes and explicit-manifest lookup from another checkout |
| Task discard | Published deliverable and infrastructure dry-runs, explicit agent review handoff, exact-ID confirmation from the shared checkout, leased network-branch deletion, local branch/directory/worktree/private-state cleanup, and permanent unreferenced S3-path purge | Missing/stale confirmation plans, changed refs, merged-task refusal, additional-scope/index restoration, wrong-ID/current-directory guards, remote rejection rollback, and pointer-version parsing |
| Scope authorization | Default task-only publication, unrelated overlay exclusion, one-shot root scope requiring a reason, authorization trailer, infrastructure out-of-scope refusal, and refusal of scoped content hidden only by `.git/info/exclude`, including a wholly ignored directory Git reports collapsed, followed by clean publication once the task carries the rule | Overlapping-scope refusal, symlink traversal, symbolic links escaping the repository while in-tree links still publish, the refusal naming each task kind's own workplace, a carried ignore rule outranking a global excludes rule, an unpublished rule in a tracked ignore file staying machine-local, the product's own rules never refusing a repository's first publication, nested Git checkout/gitlink, multiple include normalization, and path parser permutations |
| Git-only publication | `plan`, `publish --dry-run`, first publish, repeat no-op, network ref verification, Unicode/space paths, ordinary Git move, and review handoff | Ignored-directory pruning, explicit Git directory inheritance, sticky published Git placement, identity/message validation, and private-index details |
| Transaction concurrency | A separate process holding the common repository lock blocks another transaction and release resumes normally | Cross-command lock variants and per-storage-boundary lock naming |
| Explicit and automatic placement | Collaboration/artifact semantics, aggregate boundary metrics, tiny-S3 warnings, 1–10 MiB semantic review, large-file automatic S3, explicit large-file Git, status/plan explanations, reset stickiness, Git-to-S3, and S3-to-Git | Multi-path local conversion rollback, exact threshold boundaries, recursive explicit Git, missing S3 configuration, the task-record guard applying to content routed to S3, and S3 boundary paths the storage engine cannot address, refused by automatic and explicit placement, by every command that would address metadata left at one, and ahead of the cloud-usage gate in `cloud_usage_approval.rs` |
| Untrack and local retention | Git and versioned S3 to persistent local placement, dry-run, unchanged payload/index/remotes before publication, published payload and pointer deletion, explicit plan reporting, no repeat uploads, protected cleanup across main and tag references, retained bytes through merge/refresh, permanent all-version purge after reference release, and final idempotency | Special-character ignore rules, shared staged-index preservation, complete Git/S3 directories and future descendants, nested-boundary refusal without partial conversion, restoring Git/S3 placement, and refresh rollback variants in `untrack_transactions.rs` and `untrack_refresh.rs` |
| Boundary integrity | Descendant inheritance plus nested set/reset refusal without remote changes | Symlink escapes and additional parent/child boundary permutations |
| Version-aware S3 | Enabled-bucket precondition, exact version IDs in metadata, exact object verification, immutable updates at live paths, directory additions/deletions, explicit remove, and permanent all-version purge for deleted, moved, renamed, discarded, or Git-transitioned paths | Metadata shape/ownership validation, pending-purge state, and remote-ref protection variants |
| Hydration | Targeted and scope-wide hydration from an empty cache, exact file/directory versions, fresh-clone hydration, dry-run, local-modification refusal, and missing exact-version failure | Invalid targets and local-filesystem remote variants |
| Publish failure ordering | Bad S3 credentials cannot move either local or remote Git refs; Git server rejection occurs only after verified S3 upload, leaves a retryable local commit, and succeeds on retry; missing local stored output is not deletion | Multi-path local metadata rollback and adapter error rendering |
| Cloud usage approval | The default build's fixed 1 GiB threshold; exact version-aware S3 accounting of unuploaded metadata, changed outputs, every retained version at a live path, and release on the retiring publication; an oversized sparse file refused by `publish` before tracking or any Git/S3 write; the pending decision in `task status` and the reminder on task-scoped commands; refusal of an approval below the threshold; `task approve-cloud-usage` recording the decision in a schema 3 task manifest without writing either remote; then, depending on `workspace-mgr --version`: below 0.4.0, `plan`, `publish --dry-run`, and `publish` refusing to publish the schema 3 manifest without tracking, pushing, or uploading anything, a reset to the threshold (`recorded`, then `unchanged`), and a publication at the default limit that carries neither trailer nor a requirement change; from 0.4.0 on, `publish --dry-run` passing under the approved limit, cleanup back within the limit, and the next publication carrying the schema 3 manifest, the `Workspace-Requirement` and `Cloud-Usage-Approval` trailers, and a raised `minimum_cli_version` in the published tree while the shared checkout and `main` stay unchanged | Size parsing and binary-unit formatting, the test-only threshold override, published/delta Git object sets, Git LFS and packed sizes, content-addressed test storage, cleanup-only and Git-history rules, blocked and dry-run approvals, every reminder surface, corrupt private state, schema 1/2/3/4 validation of the manifest approval table, the release guard for a build older than 0.4.0, a reset to the threshold returning the manifest to schema 2 and withdrawing the branch's requirement raise, `unchanged` approvals that point to `plan` for unpublished manifest changes, approvals refused outside the checkout that publishes the manifest and recorded with `--allow-non-shared-head` in an authorized alternate workflow, deliverable and infrastructure renames keeping the approval, another clone continuing from the published manifest, trailer-shaped subject lines, and late re-checks after the storage commit and after the upload in `cloud_usage_approval.rs` and `src/cloud_usage.rs` |
| Repository minimum version | A network `main` that requires a newer release makes `refresh` and `refresh --dry-run` refuse before moving local `main`, the index, or the configuration, and makes `plan` and `publish` refuse without moving the task branch while local task commands keep working | A local declaration refusing every repository command, including `instructions`, while `doctor` reports `cli-version`; unknown future fields; semantic-version precedence, pre-release builds meeting their own release, and invalid declarations including pre-releases; `init` preservation; `task create` (both kinds, also as a rehearsal) and `task discard` refusing a newer fetched base, `task rename`, `plan`, and `publish` refusing a newer base or task branch, and `doctor` reporting the last fetched base declaration; raises that report the previous declaration; concurrent raises producing identical configuration blobs that merge cleanly; an unmerged raised branch following a shared branch that a later release raised further and merging cleanly; a withdrawn raise leaving the shared configuration untouched after the merge; a withdrawal after a rebase merge keeping the declaration the shared branch carries; untouched branches never changing the configuration; authorized configuration changes, including comment-only ones, that are kept and only raised, also to follow a shared branch raised further; the release guard, which offers removing an approval only for the task's own manifest; infrastructure publications restoring a missing declaration in their isolated worktree; and the test-only release override in `repository_requirement.rs`, `src/transaction.rs`, and `src/config.rs` |
| Credential hygiene | Virtual credentials come only from private environment/config, are not staged, and deliberately failed S3 operations do not echo them | Credential-bearing tracked URL rejection |
| Shared-checkout refresh | Dry-run, staged-index refusal, provider failure before ref advance, ordinary add/modify/delete, tracked and untracked overlays, S3 prefetch, induced post-ref checkout failure with full rollback, success, and idempotency; an incoming boundary the storage engine cannot address, crafted on the versioned bucket, skipped and reported at dry run and at apply while the other incoming boundary hydrates, then recovered exactly as the warning states, with the move fetching through the old version ID, publication uploading the new path, every checkout hydrating it, and the old path's versions purged once no reference protects them | Incoming storage symlink traversal, file/directory type transitions, unresolved-index variants, temporary-worktree cleanup fault injection, and, for an incoming boundary the storage engine cannot address, refusal before any change when the checkout holds a payload there the incoming metadata does not describe or one without metadata, keeping one that matches, the recovery on a filesystem remote including the refusal to publish before the other boundaries are hydrated, and the rollback of a failed move of an un-hydrated boundary |
| Refresh ancestry | A real network force-push to a divergent `main` is rejected without moving local `main`, the index, or overlays | Deterministic ancestry edge cases |
| Pull-request ownership | The CLI returns a provider-neutral handoff requiring immediate deliverable review creation, turn-end synchronization, one draft PR, agent-managed metadata, user-only merge authority, and an explicit close-before-discard transition | Actual GitHub PR creation/update/close is intentionally agent-owned and outside the CLI/system E2E boundary |
| Platform and packaging | The same source is built and tested on Linux ARM64 and Apple Silicon; the Linux system job exercises network Git and versioned S3 | Native archive/installer tests and Rust 1.85 compatibility |

## Deliberate external boundaries

- Tests never contact the real B2/S3 account, read developer credentials, or
  delete real remote objects. Provider faults are exercised only against the
  disposable versioned MinIO bucket.
- The CLI does not call GitHub or another hosting API. The E2E checks the review
  handoff contract; the responsible agent owns PR title, description, state,
  head verification, and the rule that it must not merge.
- The service-backed lifecycle runs once on Linux. Apple Silicon and Linux ARM64
  run the complete isolated Rust/integration suite and native packaging checks;
  duplicating the Docker-backed MinIO lifecycle on every architecture would not
  cross a new product boundary.

When a public command, state transition, or remote failure point is added, this
matrix and either `run.py` or the named isolated coverage must change in the same
pull request.
